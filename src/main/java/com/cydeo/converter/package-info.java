/**
 * This package is converts String to Object
 * convert String (th:value="${role.id}" value is "1", "2") to Object
 * Classes we need for thymeleaf, if you work with angular then we dont need it
 * When this class methods run? When we use @ConfigurationPropertiesBinding annotation Spring will run automatically
 */

package com.cydeo.converter;